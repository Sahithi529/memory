// name - the name of the player.
// size - the board size.
// totalTime - the time as milliseconds.
// minuteAndSeconds - minuteAndSeconds as string.
export interface Accomplishment {
  name: string;
  size: number;
  totalTime: number;
  minutes: number;
  seconds: number;
}
